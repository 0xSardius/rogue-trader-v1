# --- Strategy selection ---
STRATEGY=copy-trade            # copy-trade | funding-carry | sniper

# --- SolEnrich (the brain) ---
SOLENRICH_URL=https://api.solenrich.com
SOLENRICH_INTERNAL_KEY=        # X-Internal-Key bypass (swarm calls SolEnrich free)

# --- Solana / execution ---
SOLANA_RPC_URL=
WALLET_PUBKEY=
WALLET_PRIVATE_KEY=            # NEVER commit. NEVER log. Live trading only.
JUPITER_API_KEY=              # Jupiter Ultra/Swap

# --- LLM (rug/sanity veto only) ---
ANTHROPIC_API_KEY=

# --- Ops ---
API_TOKEN=                    # dashboard bearer auth
KILL_SWITCH_SECRET=
DISCORD_WEBHOOK_URL=          # optional trade/error notifications
